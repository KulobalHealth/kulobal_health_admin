# Production builds call same-origin /api/v1/admin (Vercel proxy → Render).
# Do NOT point the browser at the Render URL in production — that triggers CORS/500s.
# REACT_APP_API_BASE_URL is only used for local development (.env).

# Optional:
# REACT_APP_ENV=production
